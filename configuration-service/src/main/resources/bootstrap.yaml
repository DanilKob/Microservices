---
spring:
  application:
    name:
      configuration-service
  profiles:
    active: clustered-instance1
    #active=dev
### Cloud config server setup
  cloud:
    config:
      server:
        encrypt:
          enabled: false
encrypt:
  key:
    secretkeyforencryption